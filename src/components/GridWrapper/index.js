import React from 'react';
import "./styles.css"
const RFGridWrapper = ({children}) => {
  return (
    <div className="rfGridWrapper">
     {children}
    </div>
  );
};

export default RFGridWrapper;
